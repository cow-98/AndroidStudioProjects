<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/iconItem"
        android:layout_width="@dimen/icon_size"
        android:layout_height="@dimen/icon_size"
        android:layout_marginLeft="10dp"
        android:scaleType="centerCrop"
        android:src="@drawable/sample_0"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/textItem1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="Name"
        android:padding="@dimen/list_item_padding"
        android:textColor="@color/black"
        android:textSize="@dimen/list_item_text_size1"
        android:layout_marginLeft="20dp"
        app:layout_constraintBottom_toTopOf="@+id/textItem2"
        app:layout_constraintStart_toEndOf="@+id/iconItem"
        app:layout_constraintTop_toTopOf="@+id/iconItem" />

    <TextView
        android:id="@+id/textItem2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="Age"
        android:padding="@dimen/list_item_padding"
        android:textColor="@color/black"
        android:textSize="@dimen/list_item_text_size2"
        android:layout_marginLeft="20dp"
        app:layout_constraintBottom_toBottomOf="@+id/iconItem"
        app:layout_constraintStart_toEndOf="@+id/iconItem"
        app:layout_constraintTop_toBottomOf="@+id/textItem1" />

</androidx.constraintlayout.widget.ConstraintLayout>